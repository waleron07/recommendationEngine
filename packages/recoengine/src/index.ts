/**
 * `recoengine` — batteries-included entry point.
 *
 * Re-exports the core plus the default plugin set. If you want a zero-dependency
 * core and hand-picked plugins, depend on `@recoengine/core` directly instead.
 *
 * @packageDocumentation
 */

export * from '@recoengine/core'
